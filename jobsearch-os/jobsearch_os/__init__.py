"""jobsearch-os (v6): local-first, resumable, approval-gated job-search OS.

Phase 1 is fully deterministic — no model/provider dependency. See
docs/INVARIANTS.md for the safety invariants that govern every module.
"""

__version__ = "0.1.0"
